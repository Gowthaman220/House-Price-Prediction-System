global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: "house_price_api"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["api:8000"]
        labels:
          app: "house-price-api"
          environment: "production"
